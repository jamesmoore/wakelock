{
  "name": "wakelock",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o src/tailwind.css --minify",
    "dev": "npx http-server -o ./src/"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.4",
    "tailwindcss": "^4.2.4"
  }
}
